apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "parca.fullname" . }}-config
  labels:
    {{- include "parca.labels" . | nindent 4 }}
data:
  parca.yaml: |-
    "debug_info":
      "bucket":
        "config":
          "directory": "./tmp"
        "type": "FILESYSTEM"
      "cache":
        "config":
          "directory": "./tmp"
        "type": "FILESYSTEM"