apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "parca.fullname" . }}-test-connection"
  labels:
    {{- include "parca.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget-agent
      image: busybox
      command: ['wget']
      args: ['parca-server:{{ .Values.agent.service.port }}']
    - name: wget-server
      image: busybox
      command: ['wget']
      args: ['parca-agent:{{ .Values.server.service.port }}']
  restartPolicy: Never
